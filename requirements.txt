demucs>=4.0.1
gradio>=5.0
# newer torchaudio versions need torchcodec (+ system ffmpeg) to write audio files
torchcodec
fastapi
uvicorn[standard]
python-multipart
librosa
openai-whisper
# sheet-music transcription. basic-pitch itself must be installed with
# --no-deps (its pins predate python 3.13; the ONNX backend works fine
# with the modern deps below):  pip install --no-deps basic-pitch
music21
onnxruntime
pretty_midi
mir_eval
resampy
